# MyBatis-Plus
mybatis-plus:
  mapper-locations: classpath:mappers/*.xml

# Web
server:
  port: 8080
  servlet:
    context-path: /api/v1

# MySQL
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/gleam_market?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4"
    username: root
    password: liao2004
    driver-class-name: com.mysql.cj.jdbc.Driver
